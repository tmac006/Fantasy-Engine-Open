{
  "name": "fantasy-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/lib/*.test.ts src/adapters/*.test.ts",
    "types": "node scripts/gen-types.mjs",
    "types:check": "node scripts/gen-types.mjs --check"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^26.1.2",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
